version: '3.8'

services:
  puzzle-maker:
    build: ..
    ports:
      - "7862:7862"
    restart: unless-stopped
    volumes:
      - ../data:/app/data
    environment:
      - GRADIO_SERVER_NAME=0.0.0.0
